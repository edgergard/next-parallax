import { scrollToAnchor } from "./scrollToAnchor";
import {
  getSessionStorage,
  removeSessionStorage,
  setSessionStorage,
} from "./storage";
import { truncate } from "./truncate";

export {
  truncate,
  scrollToAnchor,
  setSessionStorage,
  getSessionStorage,
  removeSessionStorage,
};
